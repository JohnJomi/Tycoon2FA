# Minimal pytest configuration for the Phase 0 scaffold.
# Scopes collection to tests/ so nothing under .venv/, data/ or models/ is
# ever picked up. No tests exist yet; this file adds no test behaviour.
[pytest]
testpaths = tests
